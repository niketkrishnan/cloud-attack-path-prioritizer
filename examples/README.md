# Examples

The primary example is the checked-in local fixture used by `evaluate.py`.
It demonstrates read-only cloud graph analysis, public exposure, trust relationships, privilege transitions, and explainable remediation ranking without requiring external accounts or
credentials. Add new examples with expected outcomes and a short explanation.
